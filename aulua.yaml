# yaml-language-server: $schema=https://raw.githubusercontent.com/karoterra/aviutl2-aulua/refs/heads/main/schema/aulua.schema.json
project:
  variables:
    VERSION: v0.1.0
    AUTHOR: azurite
build:
  out_dir: script_out
install:
  out_dir: script_out
scripts:
  - name: 中心ずらし_A.anm2
    sources:
      - path: script/中心ずらし_A.in.anm2
        variables:
          INFO: 中心ずらし_A
          LABEL: 配置
